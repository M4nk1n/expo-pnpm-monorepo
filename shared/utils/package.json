{
  "name": "@shared/utils",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "react-native": "src/index",
  "source": "src/index",
  "main": "build/index",
  "types": "build/index",
  "files": [
    "build"
  ],
  "scripts": {
    "prepare": "pnpm clean && pnpm build",
    "build": "tsc --build",
    "clean": "rimraf build ./tsconfig.tsbuildinfo"
  },
  "peerDependencies": {
    "react": ">=17"
  }
}
