{
  "name": "@shared/components",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "react-native": "src/index",
  "source": "src/index",
  "main": "src/index",
  "scripts": {
    "build": "rm -rf build tsconfig.tsbuildinfo && tsc --build"
  },
  "peerDependencies": {
    "react": ">=17",
    "react-native": ">=0.68.2"
  }
}
